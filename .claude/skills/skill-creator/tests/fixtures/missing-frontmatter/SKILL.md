# Missing Frontmatter

This file has no YAML frontmatter at all, so the harness cannot read a name or
description and would fail to load it as a skill. It anchors the bottom of the
score range in the test suite.
